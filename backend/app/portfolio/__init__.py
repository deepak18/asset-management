"""Portfolio domain — ledger, allocation, investor-centric returns (XIRR).

Only pure, deterministic math lives here for now (calculators + schemas).
No DB, no network, no MCP — those arrive behind provider interfaces later.
"""

